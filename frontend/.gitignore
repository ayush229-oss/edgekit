node_modules/
.next/
out/
.DS_Store
*.log

# Environment files. `.env.example` is the only one that belongs in git —
# everything else (including `vercel env pull` dumps like .env.vercel-prod)
# holds live secrets and must never be committed.
.env
.env.*
!.env.example

# Vercel project linkage — machine-local, regenerated by `vercel link`.
.vercel
